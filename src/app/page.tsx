import Hero from '@/components/hero';
import Services from '@/components/services';

const Page = () => {
    return (
        <div>
            <Hero />
            <Services />
        </div>
    );
};

export default Page;
